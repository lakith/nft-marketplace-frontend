import {createStyles, makeStyles} from '@mui/styles';

export const collectionCardPageStyles = makeStyles((theme) => createStyles({
    buttonWrapper: {
        marginTop: '5% !important',
        width: '100%',
    },
}));
